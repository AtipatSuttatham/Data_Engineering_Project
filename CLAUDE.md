# CLAUDE.md: ข้อตกลงโปรเจกต์ Doi Brew ETL

ไฟล์นี้คือข้อตกลงการทำงานของโปรเจกต์ Claude ต้องอ่านและทำตามทุกครั้งที่เริ่มงาน ถ้าข้อตกลงใดต้องเปลี่ยน ให้ถามผู้ใช้ก่อนแล้วแก้ไฟล์นี้ อย่าทำต่างจากที่เขียนไว้โดยไม่บอก

## 1. ภาพรวมโปรเจกต์

- **วิชา:** 204426 Data Engineering เทอม 1/2569 (รายละเอียดโจทย์และเกณฑ์อยู่ใน `group_project_169 (2).pdf`)
- **หัวข้อ:** ETL Pipeline รวมข้อมูลยอดขายร้านกาแฟสมมุติ "Doi Brew" 3 สาขาในเชียงใหม่ (นิมมาน, มช., ท่าแพ)
- **รูปแบบ:** เว็บแอป Streamlit ที่อัปโหลดข้อมูล → ตรวจคุณภาพ → ทำความสะอาด → แปลง → โหลดเข้า SQLite → Dashboard → โมเดล
- **โมเดลโบนัส:** Recommendation (เมนูที่มักซื้อคู่กัน) และ Regression (ทำนายยอดขาย)
- **เกณฑ์คะแนน (15%):** ให้คะแนน**เฉพาะงานด้าน DE** โดยดูว่า**เทคนิคเหมาะกับลักษณะและปัญหาของข้อมูลหรือไม่** ความสวยของ UI ไม่มีคะแนน
- **ผู้พัฒนา:** ผู้ใช้เป็นคนเขียนโค้ดคนเดียว กลุ่มมี 3 คน แต่ทุกคนต้องนำเสนอและตอบคำถามได้ คำอธิบายจึงต้องทำให้คนที่ไม่ได้เขียนโค้ดเข้าใจด้วย

## 2. วิธีทำงานร่วมกัน

- **หยุดขออนุมัติทุกโมดูล:** ทำเสร็จ 1 โมดูลแล้วอธิบายและรอผู้ใช้อนุมัติ ห้ามเริ่มโมดูลถัดไปเอง ลำดับโมดูลอยู่ในหัวข้อ 5
- **ก่อนเริ่มแต่ละโมดูล** บอกสั้น ๆ ว่าจะทำอะไร ใช้เทคนิคอะไร
- **หลังจบแต่ละโมดูล** ต้องมี 1) สรุปสิ่งที่ทำ 2) วิธีรันหรือทดสอบ 3) บันทึกสำหรับเล่มรายงาน (หัวข้อ 8)
- **ภาษา:** คุยและอธิบายเป็นภาษาไทย ละเอียดและเข้าใจง่าย ใช้ศัพท์เทคนิคภาษาอังกฤษให้ตรงกับสไลด์ Lecture และอธิบายศัพท์ครั้งแรกที่ใช้
- **ห้ามเดา:** ถ้าไม่แน่ใจหรือมีทางเลือกที่มีผลต่อคะแนนหรือโครงสร้าง ให้ถามพร้อมบอกคำแนะนำ

## 3. ขอบเขตเทคนิค

ใช้**เฉพาะเทคนิคที่สอนใน Lecture (`Lecture/ch-1` ถึง `ch-7`) และ Lab (`Lab/Lab_01` ถึง `Lab_11`)** ยกเว้นโมเดลโบนัส ทุกเทคนิคต้องระบุได้ว่า
1. **แก้ปัญหาอะไรของข้อมูล** (ห้ามใช้เทคนิคเพียงเพื่อให้ครบจำนวน)
2. **มาจากบทไหนหรือ Lab ไหน**
3. **ทำไมเลือกวิธีนี้แทนวิธีอื่น** (เช่น เลือก median แทน mean เพราะข้อมูลเบ้)

| บท | เทคนิคที่ใช้ได้ |
|---|---|
| Ch1 ETL | Extract / Transform / Load, Initial load, Incremental load, Full refresh, Load verification |
| Ch2 Transformation | Data discovery, Translation mapping, JSON → ตาราง, แปลงวันเวลา, Filtering, Aggregation, Attribute construction, Discretization, Enrichment / split, Min-Max, Z-score, Anonymization, One-hot / Dummy / Ordinal encoding |
| Ch3 Data Quality | 6 มิติ (Completeness, Uniqueness, Consistency, Validity, Accuracy, Timeliness), กระบวนการ 6 ขั้น, Simple ratio, Min/Max operation, Weighted average |
| Ch4 Cleaning | กระบวนการ 5 ขั้น, Listwise deletion, Mean / Median / Mode imputation, Regression imputation, Stochastic regression imputation, Dummy variable adjustment, Z-score, DBSCAN, Isolation Forest |
| Ch5 Integration | รวมหลายแหล่งเข้า repository เดียว, Schema mapping, Data warehouse |
| Ch6 Exploration / Viz | สถิติเชิงพรรณนา, Skewness, Kurtosis, IQR, Histogram, Boxplot, Violin, Scatter, Heatmap, Bar, Line, Moving average |
| Ch7 Feature Extraction | Bag of Words, TF-IDF (ใช้ถ้ามีข้อมูลข้อความ) |

**โมเดลโบนัส** ใช้เทคนิคนอกวิชาได้ แต่ต้องอธิบายหลักการอย่างละเอียดในเล่ม

## 4. เครื่องมือและสภาพแวดล้อม

- **Python 3.13** เรียกด้วย `py -3.13` (เครื่องนี้มี Python 3.11 ด้วย แต่ไม่มี pandas)
- ไลบรารี: pandas, numpy, scikit-learn, scipy, matplotlib, seaborn, plotly, streamlit, openpyxl, sqlite3 (มากับ Python), pytest
- **ห้ามเพิ่มไลบรารีใหม่โดยไม่ขออนุญาต** ถ้าเพิ่มต้องอัปเดต `requirements.txt`
- คำสั่งหลัก (จะใช้ได้เมื่อสร้างโมดูลเสร็จ):
  - สร้างข้อมูล: `py -3.13 data_generator/generate.py`
  - รันแอป: `py -3.13 -m streamlit run app.py`
  - ทดสอบ: `py -3.13 -m pytest tests`

## 5. โครงสร้างโค้ดและลำดับโมดูล

```
data engineering project/          ← root ของ git repo
├── CLAUDE.md
├── README.md                      วิธีติดตั้งและรัน (สำหรับอาจารย์)
├── requirements.txt
├── data_generator/generate.py     [โมดูล 1] สร้างข้อมูลสมมุติ + ใส่ปัญหา
├── data/raw/                      ข้อมูลดิบ 3 สาขา + สมาชิก + รหัสสินค้า
├── data/processed/                ผลลัพธ์ (ไม่ commit) เช่น sales_staging.csv, sales_clean.csv, transform/*.csv
├── pipeline/
│   ├── extract.py                 [โมดูล 2] อ่านไฟล์ + Schema mapping + แปลงค่ากลุ่ม A + Integration → staging
│   ├── quality.py                 [โมดูล 3] วัดคุณภาพ 6 มิติ
│   ├── clean.py                   [โมดูล 4] ข้อมูลซ้ำ, validity, ค่าว่าง, outlier, ชื่อเมนูสะกดผิด (กลุ่ม B, C)
│   ├── transform.py               [โมดูล 5] enrichment, สร้างคอลัมน์ใหม่, discretization, encoding, normalization, anonymization, aggregation
│   ├── load.py                    [โมดูล 6] โหลดเข้า SQLite + ตรวจสอบหลังโหลด
│   ├── recommend.py               [โมดูล 7] Recommendation
│   └── forecast.py                [โมดูล 8] Regression
├── app.py                         [โมดูล 9] หน้าเว็บ Streamlit
├── evaluation/                    วัดความแม่นของ pipeline เทียบกับเฉลย (อ่าน data/generator_log/ ได้ แยกจาก pipeline)
├── tests/                         pytest แบบเบา (เขียนคู่กับแต่ละโมดูล)
├── docs/
│   ├── data_issues.md             รายการปัญหาที่ใส่ในข้อมูล (จากโมดูล 1)
│   └── report_notes/              บันทึกสำหรับเล่ม 1 ไฟล์ต่อโมดูล
└── Lecture/, Lab/, *.pdf          เนื้อหาวิชา (ไม่ commit)
```

[โมดูล 10] README + requirements + ตรวจว่ารันจาก clone ใหม่ได้

**หลักแบ่งงานระหว่างโมดูล:** ปัญหากลุ่ม A (ต่างกันทั้งไฟล์อย่างเป็นระบบ) แก้ในโมดูล 2 / ปัญหากลุ่ม B, C (ผิดบางแถว) โมดูล 2 ห้ามแตะ ให้โมดูล 3 วัด แล้วโมดูล 4 แก้ ถ้าโมดูล 2 แปลงค่าไม่ได้ ให้ปล่อยว่าง เก็บค่าดิบไว้ และติดธงใน `extract_flags`

## 6. กฎการเขียนโค้ด

- **ส่วน DE อยู่ใน `pipeline/` เท่านั้น** เป็นฟังก์ชัน Python ล้วน ห้าม import streamlit ใน `pipeline/` เพื่อให้เปลี่ยนเป็น Flask ภายหลังได้
- `app.py` ทำหน้าที่แค่เรียกฟังก์ชันใน `pipeline/` แล้วแสดงผล
- ฟังก์ชันรับ DataFrame แล้วคืน DataFrame ใหม่ ห้ามแก้ข้อมูลต้นฉบับ (ใช้ `.copy()`) เพื่อให้แสดง Before / After ได้
- ฟังก์ชันทำความสะอาดควรคืน**รายงานสิ่งที่ทำ**ด้วย (เช่น ลบข้อมูลซ้ำไปกี่แถว) เพื่อแสดงบนเว็บและใส่ในเล่ม
- **ห้ามใช้ path แบบเต็ม** (`C:\Users\...`) ให้ใช้ `pathlib` อ้างอิงจากตำแหน่งไฟล์
- **กำหนด random seed คงที่** (`SEED = 42`) ทุกจุดที่มีการสุ่ม เพื่อให้รันซ้ำได้ผลเหมือนเดิม
- ชื่อตัวแปร ฟังก์ชัน และไฟล์เป็นภาษาอังกฤษ ส่วน **comment และ docstring เป็นภาษาไทย** อธิบายว่าทำอะไรและแก้ปัญหาอะไร
- เขียนให้อ่านง่าย เพราะเพื่อนในกลุ่มต้องอธิบายโค้ดตอนนำเสนอได้

## 7. ข้อมูล

- เป็นข้อมูลสมมุติทั้งหมดที่สร้างด้วย `data_generator/generate.py` (seed คงที่)
- รูปแบบต่างกันตามสาขา: นิมมาน = CSV, มช. = Excel, ท่าแพ = JSON (ซ้อนหลายชั้น) + ไฟล์สมาชิก + ตารางรหัสสินค้า
- ปัญหาที่ใส่ในข้อมูลต้อง**สมจริง** และบันทึกใน `docs/data_issues.md` ทุกรายการ ระบุ: ปัญหาอะไร, อยู่ในไฟล์หรือคอลัมน์ไหน, จำนวนประมาณเท่าไร, จะแก้ด้วยเทคนิคอะไร
- ห้ามใช้ข้อมูลส่วนบุคคลจริง ชื่อและเบอร์โทรต้องสร้างขึ้นเอง
- **`data/generator_log/` คือไฟล์เฉลย** (รายการปัญหาที่ใส่ + ข้อมูลสะอาด) **โค้ดใน `pipeline/` และ `app.py` ห้ามอ่านไฟล์ในโฟลเดอร์นี้เด็ดขาด** ให้ใช้เฉพาะใน `tests/` และ `evaluation/` (วัดความแม่นของ pipeline) เพื่อไม่ให้ผลลัพธ์ดูเหมือนโกง และให้ผลวัดความแม่นมีความหมาย

## 8. บันทึกสำหรับเล่มรายงาน

หลังจบแต่ละโมดูล ให้สร้าง `docs/report_notes/<ลำดับ>_<ชื่อโมดูล>.md` เป็นภาษาไทย ทุกเทคนิคต้องมี:
1. **ปัญหาของข้อมูล** ที่พบ พร้อมตัวเลข
2. **เทคนิคที่ใช้** + บทหรือ Lab อ้างอิง + หลักการสั้น ๆ
3. **วัตถุประสงค์และเหตุผล** ที่เลือกวิธีนี้
4. **Input → Process → Output** พร้อมตัวอย่างข้อมูลก่อนและหลัง
5. **ภาพหน้าจอที่ควรเก็บ** (ระบุว่าหน้าไหน ส่วนไหน)
6. **คำถามที่อาจารย์อาจถาม** พร้อมคำตอบ (สำหรับเตรียมช่วงถามตอบ)

## 9. การทดสอบ

- ใช้ pytest แบบเบา เขียนคู่กับแต่ละโมดูล ทดสอบเฉพาะกฎสำคัญ เช่น หลัง clean ต้องไม่มีข้อมูลซ้ำ, อายุต้องอยู่ในช่วงที่กำหนด, วันที่เป็นรูปแบบเดียวกัน, ยอดรวมหลังโหลดตรงกับก่อนโหลด
- ต้องรัน `py -3.13 -m pytest tests` ผ่านก่อนรายงานว่าโมดูลเสร็จ ถ้าไม่ผ่านให้บอกตามจริง

## 10. Git และ GitHub

- Repo: https://github.com/AtipatSuttatham/Data_Engineering_Project.git root ของ repo คือโฟลเดอร์นี้
- **Commit ลง `main` โดยตรง** (ผู้ใช้เป็นคนพัฒนาคนเดียว)
- Commit และ push **หลังผู้ใช้อนุมัติโมดูลแล้วเท่านั้น** 1 โมดูลต่อ 1 commit เป็นอย่างน้อย
- Commit message มี 2 ส่วน:
  - **หัวเรื่อง (บรรทัดแรก):** ภาษาอังกฤษ สั้นและชัดเจน เช่น `Add data generator with injected quality issues`
  - **Description (ตั้งแต่บรรทัดที่ 3):** **ภาษาไทย ละเอียดและเข้าใจง่าย** อธิบายว่า commit นี้เพิ่มหรือแก้อะไร, ทำไม (แก้ปัญหาอะไรของข้อมูล / ใช้เทคนิคจากบทไหน), ไฟล์สำคัญที่เกี่ยวข้อง และวิธีรันหรือทดสอบ ถ้ามี
- ห้าม commit: `Lecture/`, `Lab/`, `*.pdf`, `data/processed/`, `*.db`, `__pycache__/`, `.ipynb_checkpoints/` (กำหนดใน `.gitignore`)
- ห้ามใช้คำสั่งที่ลบประวัติ (`push --force`, `reset --hard`) โดยไม่ขออนุญาต

## 11. กำหนดส่งและสิ่งที่ต้องส่ง

- **อา. 4 ต.ค. 2569:** `ชื่อกลุ่ม.docx` (เล่ม), `ชื่อกลุ่ม.zip` (โค้ดทั้งหมด), `ชื่อกลุ่ม.pptx` (สไลด์) บน Google Drive + สมาชิกทุกคนส่งชื่อกลุ่มใน Mango
- **6 / 9 / 16 ต.ค. 2569:** นำเสนอไม่เกิน 12 นาที + ถามตอบ 2 นาที โดยต้องมี Demo
- เป้าหมายภายใน: โค้ดทุกโมดูลเสร็จภายใน **30 ก.ย. 2569** เพื่อให้มีเวลาเขียนเล่มและทำสไลด์
